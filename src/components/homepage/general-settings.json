{
  "collectionName": "components_homepage_general_settings",
  "info": {
    "displayName": "General Settings",
    "icon": "settings",
    "description": "General homepage settings including SEO and status"
  },
  "options": {},
  "attributes": {
    "pageTitle": {
      "type": "string",
      "required": true,
      "maxLength": 60,
      "description": "SEO page title (max 60 characters)"
    },
    "metaDescription": {
      "type": "string",
      "required": true,
      "maxLength": 160,
      "description": "SEO meta description (max 160 characters)"
    },
    "activeStatus": {
      "type": "boolean",
      "required": true,
      "default": true,
      "description": "Whether the homepage is active and should be displayed"
    },
    "lastUpdated": {
      "type": "datetime",
      "required": false,
      "description": "Auto-generated timestamp of last update"
    }
  }
}
